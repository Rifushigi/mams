# Generate with: python -m utils.crypto
FACE_ENCODING_KEY=

# Must match the backend's ACCESS_TOKEN_SECRET
ACCESS_TOKEN_SECRET=

# Service credential for backend-to-ML calls that carry no enduser context.
ML_SERVICE_TOKEN=

BACKEND_URL=http://localhost:3000
